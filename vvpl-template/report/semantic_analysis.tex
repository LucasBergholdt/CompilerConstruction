\begin{comment}
Here you explain the technical work you have carried out. You should present the overall
architecture of your interpreter. Try to explain the development choices you made taking
into account important properties, such as reusability and performance.

If you have implemented specific algorithms or used functionality not covered in the lecture,
please explain it here. Which kind of design decisions did you take? Answer the following
questions in the respective sections.

• How do you perform the semantic analyses?
\end{comment}
\begin{comment}
- ScopeAnalyzer (interessante ting at dykke ned i)
    kortfattet:
        - Forskellige typer environments (symbol tables forskelle)
        - Håndtering af FUNCTIONS (is_function_env) og hvordan denne bruges. 
- TypeAnalyzer
    - introducer nye Type samt type.UNKNOWN anvendelse
    - Håndtering af Functions generelt (always returns og currentFuncReturnType)
\end{comment}
After having making sure that the program is syntactically correct, we want to confirm that our context-sensitive grammar is also semantically correct, 
i.e. perform context-sensitive checks and interpret the meaning of the program at its' syntactic level.
VVPL is a statically typed language, which usually means that we perform context-sensitive checks at compile-time, but due to the fact taht VVPL is an interpreted language,
the property of being statically typed means that we perform the semantic analysis before runtime.

VVPL uses lexical scoping (i.e. curly brackets indicate a new scope) and symbol tables to perform context-sensitive checks.
We chose to implement two separate symbol tables for variables and functions as the mappings and the functionality for these symbol tables would differ.
A function table does not need an outer scope due to it only being used in the global scope, and it does not need the ability to assign a new function to a function identifier.

\paragraph{Design choices in Scope Analysis}\mbox{}\\
Usually, a variable is not allowed to shadow another variable in an outer scope. We implement this by making the \code{define} function look recursively in all outer tables
to check if the definition is legal.
In functions however, variables can shadow other variables defined outside of the function. This lead to the design decision of initializing functions with a fresh symbol table that has no outer table
so that bodies in functions are completely isolated, having no contact to the domain outside of the function.

This design solution impacted the solution to the issue of functions only being able to be defined in the global scope. A solution to this could originally be to 
check if the current table had an outer table, but due to both the global symbol table as well as function tables having no outer scope, this would no longer be possible.
Therefore the new symbol table attribute \code{isGlobal} was introduced, and functions defined in other tables than these would lead to an error.

Another key issue was the addressing of \code{ReturnStmt}s whose occurrence is only allowed in a function declaration. One could think that it would suffice to check if the current block 
was a direct descendant from a FunctionStmt by checking if the symbol table had the attributes \code{isGlobal = false;} and \code{outer = null;}.
This would however not suffice since nested blocks of these blocks also would be allowed to contain \code{ReturnStmt}s.
We therefore introduced the new variable \code{is\_function\_env} that is set before analysing a block in \code{VisitFunctionStmt}. This way,
\code{VisitReturnStmt} returns an error if this variable is true:
\begin{lstlisting}[language=java]
is_function_env = true;
analyse(functionStmt.body);
is_function_env = false;
\end{lstlisting}
\begin{lstlisting}[language=java]
if (!is_function_env) {
    VVPLController.error(returnStmt.returnKeyword.line, ErrorTypeStrings.SCOPE_ERROR, "Return statement cannot occur outside of function.");
}
\end{lstlisting}
% Evt lidt om: No statements can follow a return statement
% Evt lidt om: Out-of-order declarations and calls are allowed, i.e. a function call can precede its' declaration
\begin{comment}
In visitBlockStmt we ensure that no statements follow a return statement by checking that a returnStmt cannot be a non-final statement of its' containing block:
\begin{lstlisting}[language=java]
for (int i = 0; i < num_statements; i++) {
    if (blockStmt.stmts.get(i) instanceof ReturnStmt && i != num_statements - 1 ) {
        VVPLController.error(((ReturnStmt)blockStmt.stmts.get(i)).returnKeyword.line, ErrorTypeStrings.SCOPE_ERROR, "Return statement must be last statement of block.");
        return null;
    }
}
\end{lstlisting}

Lastly, we enforce out-of-order declarations and calls by iterating through the list of given statements and analyse function declarations prior to any other statement in ScopeAnalyzer.analyse():
\begin{lstlisting}[language=java]
public void analyse() {        
    ListIterator<Stmt> stmts = program.listIterator();

    while (stmts.hasNext()) {
        Stmt currentStmt = stmts.next();
        if(currentStmt instanceof FunctionStmt) {
            analyse(currentStmt);
            stmts.remove(); 
        }
    }
    // Analyse rest of program
    for (Stmt stmt : program) {
        analyse(stmt);
    }
    return;
}
\end{lstlisting}
\end{comment}

\paragraph{Design choices in Type Analysis}\mbox{}\\
We have constructed a separate class for executing the type analysis. This class could have been merged with the class of ScopeAnalyzer (i.e. a SemanticAnalyzer) but SOLID principles such as the Single Responsibility Principle suggests that responsibilities should be delegated to each their class.
Therefore we build the symbol tables from scratch again in order to build a context-sensitive environment required for the type analysis, but we do not need to error handle scope issues since those have been handled already.
This results in the usage of a much simpler SymbolTable as well as a new FuncSymbolTable where all error handling is omitted.

For the type analysis, an enum Type class have been implemented. The types are self-explanatory, while Type.UNKNOWN we use for two different cases:
\begin{itemize}
    \item Functions that do not have a type declared are assumed to return null. In this context, Type.UNKNOWN represents the value null
    \item Upon encountering an error in the type analysis, we return this value, so that calls higher up the call stack knows that an error was made and has already been processed. This way we avoid reporting cascading errors.
\end{itemize}

Having that cleared out, we proceed a particularly challenging issue we encountered during the development of the type analyzer:
\begin{itemize}
    \item A function declaration must in all cases return the declared type. It cannot miss a return statement or return the wrong type.
\end{itemize}
Enforcing that a function is guaranteed to return the declared type in all cases require extra handling. The requirement can be subdivided into two requirements:
\begin{itemize}
    \item Return statements within a function must all return the declared type
    \item A function with a declared type must always guarantee to return nevermind which branch it chooses to take
\end{itemize}
To tackle the first requirement we introduced the global variable \code{currentFuncReturnType} which is set equal to the declared type within the function declaration. This is done in \code{VisitFunctionStmt} before analysing the body:
\begin{lstlisting}[language=java]
if (functionStmt.typeToken != null) {
    currentFuncReturnType = convertVariableType(functionStmt.typeToken.type);
}
else {
    currentFuncReturnType = Type.UNKNOWN; // function is expected to return null. only "return;" is allowed
}
\end{lstlisting}
This way \code{VisitReturnStmt} enforces the first requirement by comparing \code{currentFuncReturnType} to the type of the returned expression, returning an error upon mismatch.

The second requirement of having a function with a declared type always return nevermind which branch it chooses to take is also enforced in the statically typed language Java and is appropiately enforced in VVPL as well.
\code{VisitFunctionStmt} uses the helper function \code{alwaysReturns} for the task:
\begin{lstlisting}[language=java]
if (functionStmt.typeToken != null && !alwaysReturns(functionStmt.body)) {
    VVPLController.error(functionStmt.name.line, ErrorTypeStrings.TYPE_ERROR, "Function must return a value in all code paths");
}
\end{lstlisting}

\begin{lstlisting}[language=java]
if (functionStmt.typeToken != null && !alwaysReturns(functionStmt.body)) {
    VVPLController.error(functionStmt.name.line, ErrorTypeStrings.TYPE_ERROR, "Function must return a value in all code paths");
}
\end{lstlisting}

\code{alwaysReturns} is a recursive function that evaluates whether a function with a declared type contains a return statement within all possible paths.
There are four cases to evaluate.
\begin{itemize}
    \item If a return statement occurs in a branch, this branch is guaranteed to return. This is the base case.
    \item An if-else statement is only guaranteed to return if both the then branch and the else branch returns.
    \begin{itemize}
        \item If the else branch is non-existant, the function can potentially skip the if-branch, therefore not returning.
    \end{itemize}
    \item A block statement (which also constitutes the body of a function)is only guaranteed to return if any statement within the block are guaranteed to return.
    \begin{itemize}
        \item If a block statement contains a return statement at its' current level, it is guaranteed to return.
        \item If a block statement contains an if-else statement, this statement has to return if there are no other statements that are guaranteed to return.
        \item If a block statement contains another block statement, this statement is indeed required to return if there are no other statements that always return.
    \end{itemize} 
    \item Any other kind of statement cannot be guaranteed to return.
\end{itemize}

With the help of \code{currentFuncReturnType} and \code{alwaysReturns}, functions are always guaranteed to return, and all possible return values matches the declared type.
